<Window x:Class="sadnerd.io.ATAS.MockChartApp.MultiChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:sadnerd.io.ATAS.MockChartApp"
        Title="ATAS Mock Chart" 
        Height="900" Width="1600"
        Background="#1a1a2e"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3a3a5c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#5a5a8c"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,3"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,3"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Top Control Bar -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#2a2a4e" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Layout:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button Content="1x1" Click="Layout1x1_Click"/>
                <Button Content="2x1" Click="Layout2x1_Click"/>
                <Button Content="2x2" Click="Layout2x2_Click"/>
                <Button Content="3x2" Click="Layout3x2_Click"/>
                
                <TextBlock Text="Add:" Foreground="White" VerticalAlignment="Center" Margin="20,0,10,0"/>
                <Button Content="+ Chart" Click="AddChart_Click"/>
                
                <TextBlock Text="Sync:" Foreground="White" VerticalAlignment="Center" Margin="20,0,10,0"/>
                <CheckBox x:Name="SyncCrosshairCheck" Content="Crosshair" Foreground="White" 
                          VerticalAlignment="Center" IsChecked="True"/>
                
                <!-- Settings Button -->
                <Button Content="⚙ Settings" Click="ToggleSettings_Click" Margin="30,3,3,3"/>
            </StackPanel>
        </Border>
        
        <!-- Chart Container -->
        <Grid x:Name="ChartContainer" Grid.Row="1" Grid.Column="0"/>
        
        <!-- Settings Panel (Right Side) -->
        <Border x:Name="SettingsPanel" Grid.Row="1" Grid.Column="1" 
                Background="#2a2a4e" Width="280" Visibility="Collapsed">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <TextBlock Text="Chart Settings" FontWeight="Bold" Foreground="White" 
                               FontSize="14" Margin="0,0,0,10"/>
                    
                    <!-- Selected Chart Info -->
                    <TextBlock x:Name="SelectedChartLabel" Text="No chart selected" 
                               Foreground="#66BB6A" Margin="0,0,0,10"/>
                    
                    <!-- Chart Data Settings -->
                    <TextBlock Text="Data" Foreground="#aaa" Margin="0,10,0,5" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,3">
                        <TextBlock Text="Load days:" Foreground="White" VerticalAlignment="Center" Width="80"/>
                        <ComboBox x:Name="DaysCombo" Width="80" SelectionChanged="DaysCombo_SelectionChanged">
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="5"/>
                            <ComboBoxItem Content="10"/>
                            <ComboBoxItem Content="20"/>
                            <ComboBoxItem Content="30"/>
                        </ComboBox>
                    </StackPanel>
                    
                    <!-- Separator -->
                    <Border Height="1" Background="#5a5a8c" Margin="0,10,0,10"/>
                    
                    <!-- Active Indicators -->
                    <TextBlock Text="Indicators" Foreground="#aaa" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <CheckBox x:Name="KeyLevelsCheck" Content="Key Levels" 
                              Click="IndicatorToggle_Click" Tag="KeyLevels"/>
                    <CheckBox x:Name="PvsraCandlesCheck" Content="PVSRA Candles" 
                              Click="IndicatorToggle_Click" Tag="PvsraCandles"/>
                    <CheckBox x:Name="EmaCloudCheck" Content="EMA with Cloud" 
                              Click="IndicatorToggle_Click" Tag="EmaWithCloud"/>
                    
                    <!-- Separator -->
                    <Border Height="1" Background="#5a5a8c" Margin="0,10,0,10"/>
                    
                    <!-- Dynamic Indicator Properties Container -->
                    <StackPanel x:Name="IndicatorPropertiesPanel"/>
                    
                    <!-- Apply to All button -->
                    <Button Content="Apply to All Charts" Click="ApplyToAllCharts_Click" 
                            HorizontalAlignment="Stretch" Margin="0,15,0,5"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
